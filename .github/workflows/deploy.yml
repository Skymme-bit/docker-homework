- name: 🔍 Debug SSH Connection
  uses: appleboy/ssh-action@v0.1.4
  with:
    host: ${{ secrets.SOLACE_HOST }}
    username: ${{ secrets.SOLACE_USER }}
    key: ${{ secrets.SSH_PRIVATE_KEY }}
    script: |
      echo "✅ Connected to Solace!"
      whoami
      hostname
      ls -la ~
