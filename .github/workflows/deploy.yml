name: Deploy to Solace Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: self-hosted

    steps:
      - name: 🔍 Debug Secret Length
        shell: pwsh
        run: |
          Write-Output "Docker Username: ${{ secrets.DOCKER_USERNAME }}"
          Write-Output "Password Length: $(${#${{ secrets.DOCKER_PASSWORD }}})"
