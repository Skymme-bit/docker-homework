name: Self-Hosted Runner Docker Test

on:
  push:
    branches:
      - main

jobs:
  docker-login-test:
    runs-on: self-hosted
    steps:
      - name: Check system info
        run: |
          echo "Running on: ${{ runner.os }}"
          docker --version

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        run: |
          echo "dckr_pat_-VAcu74zhJRyEY8oyoZxGUcbeqQ" | docker login -u "jadevaillantrit" --password-stdin

      - name: Verify Docker login
        run: |
          docker info
