name: Self-Hosted Runner Docker Test

on:
  push:
    branches:
      - main

jobs:
  docker-login-test:
    runs-on: self-hosted
    steps:
      - name: Debug Docker Username
        shell: powershell
        run: |
          Write-Output "Docker Username: ${{ secrets.DOCKER_USERNAME }}"
